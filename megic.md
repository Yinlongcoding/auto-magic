# Auto Magic 当前方案事实来源

> 文档版本：2.0
> 建立日期：2026-08-24
> 最近更新：2026-09-01
> 当前状态：Chrome 扩展列表抓取与 Windows 桌面端最小闭环已完成；Ozon 动态 Schema 与详情事实覆盖验证进入测试
> 当前产品方向：基于用户当前 Chrome 会话采集 1688 商品数据，并逐步接入商品处理与 Ozon 上架流程

## 0. 2026-08-28 当前有效决策

本节是当前实施事实来源。下方“Python 脚本启动器”内容保留为历史方案附录；凡与本节或 [`DESKTOP_FOUNDATION.md`](./DESKTOP_FOUNDATION.md) 冲突的内容，均以本节和桌面端基线文档为准。

### 0.1 当前进度

- Chrome 扩展开发版 v0.1.10 已完成商品列表抓取；已验证关键词“连衣裙”可取得与页面一致的 60 条数据。
- 列表字段包括跳转链接、展示图片、纯文字名称和价格。
- 扩展可复用当前窗口中关键词一致的搜索结果页；关键词不一致时替换搜索词并重新搜索。
- 列表懒加载通过分步滚动触发，抓取后页面返回顶部。
- 详情页隐藏式读取与默认导出列表第二个详情页的方案已经开始验证。
- Native Messaging Host、Windows Named Pipe 和 WPF 桌面端搜索闭环已经实现并通过当前阶段验收。
- 桌面端已加入 CNY 成本核算与商品筛选测试表单；应用内直接调用无密钥的 ExchangeRate-API 开放接口读取 CNY→USD/RUB 每日参考汇率。
- 采购成本由用户售价、目标利润率与其他成本动态反算为区间，并通过版本化 Native Messaging 协议传给 Chrome 扩展。
- Chrome 扩展进入结果页后不进行首次滚动；依次填写最低采购价、点击最高采购价输入框、填写最高采购价，再使用真实鼠标事件点击显示出的确认按钮并等待列表重载。随后按销量优先或既有价格DOM规则排序并再次等待列表重载，最后只执行一次完整懒加载并抓取JSON。
- 桌面端已加入测试用 Ozon `Client-Id`/`Api-Key` 输入，以及已选 `description_category_id`/`type_id` 的只读动态 Schema 获取；API Key 仅保存在当前用户 Windows Credential Manager 中，Client ID 与测试选择保存在本地设置中，均不进入日志，也不发送给 Native Host 或 Chrome 插件。
- 用户于 2026-08-31 提供的中文版 Ozon 品类树 JSON 已作为本地测试快照随桌面端发布。快照包含 26 个顶级目录、568 个品类节点和 7,365 个商品类型；界面将其中 542 个含商品类型的品类展平为中文完整路径，并使用“品类 → 商品类型”级联选择自动取得两个 ID，不再手工输入。中文版与此前英文版的全部 ID 结构一致。
- Chrome 扩展会在当前 Chrome 会话中用临时非激活标签打开列表第2条详情页，等待页面动态渲染后提取通用事实和产品展示图；桌面端使用确定性分层匹配输出可直接使用、待 Ozon 字典解析、低置信度待确认和真正缺失四类结果。当前语义层支持精确字段、跨语言/同义别名、安全包含关系和仅供复核的近似候选；标题只用于带证据标记的有限性别推断。
- 当前 Ozon 验证仅调用属性 Schema 只读接口，不包含商品创建、更新等写操作；本地目录仅用于测试，后续应改为从 Ozon 类目树接口更新并缓存带版本的正式目录。
- SQLite 持久化和安装更新生命周期尚未实现。

### 0.2 当前产品与安全边界

- 只支持 Windows 10/11，不规划 macOS 或 Linux 版本。
- 自动化复用用户当前 Chrome 登录状态，但不得读取、保存或上传 Cookie、Authorization 等浏览器凭据。
- Chrome 扩展和 Native Host 只接受固定、版本化命令，不允许桌面端下发任意 JavaScript、Shell 命令或不受控文件操作。
- Ozon Key 只保存在当前用户的 Windows Credential Manager 中，由桌面端直接调用 Ozon API；后端、Chrome 扩展、Native Host、SQLite 和日志均不得获取该 Key。
- 百炼等平台级长期密钥仍由后端管理，不内置到桌面端。

### 0.3 桌面端冻结基线

完整决策、目录边界、更新流程和验收门禁见 [`DESKTOP_FOUNDATION.md`](./DESKTOP_FOUNDATION.md)。核心结论如下：

- `.NET 10 LTS + WPF + C# + CommunityToolkit.Mvvm`。
- 首版仅发布 `win-x64` Self-contained 构建。
- 轻量分层单体，使用 .NET Generic Host 管理 DI、配置、日志、后台任务和退出。
- `AutoMagic.Desktop.exe` 与 `AutoMagic.NativeHost.exe` 分离；Native Host 不能访问数据库或凭据。
- Chrome Native Messaging 使用标准输入输出；Native Host 与桌面端使用仅限当前用户的 Named Pipe。
- Named Pipe 采用版本化、长度前缀 UTF-8 JSON 协议。
- SQLite + EF Core 10 + Migration；只有桌面端可以访问数据库。
- 当前用户级安装，安装到 `%LocalAppData%`，使用 HKCU 注册 Native Messaging Host，不要求管理员权限。
- 使用 Velopack 生成安装包并管理自动更新。
- 每次启动检查更新；发现新版本后后台下载，任务空闲时提示用户，确认后安装并重启。
- 用户必须提前启动桌面端；Native Host 不自动拉起桌面端。
- 点击关闭即退出；有活动任务时二次确认。“关闭到托盘”留待后续版本。
- 日志只保存在本机，不自动上传遥测；用户可主动导出诊断包。
- 项目保持闭源，当前开发和封闭测试阶段暂不做 Windows Authenticode 签名；公开发布前必须重新评估。
- 即使暂不做 Authenticode，自动更新清单仍必须使用离线私钥签名，客户端以内置公钥验签并校验 SHA-256。

### 0.4 当前实施顺序

1. 固化桌面端技术基线和项目事实来源。
2. 建立 .NET Solution 与分层项目骨架。
3. 完成 Native Messaging Host 最小闭环。
4. 完成 Named Pipe、协议校验和桌面端在线状态。
5. 接入 SQLite、任务状态、商品列表和详情数据。
6. 接入安装、更新与 Native Messaging 注册生命周期。
7. 接入 Ozon、百炼和对象存储等业务能力。
8. 建立按 Ozon 品类/类型隔离的映射方案库，并实现本地版本化、云端同步、回滚和持续学习。

### 0.5 Chrome 插件开发版本约定

- 当前插件开发版本：`v0.1.10`。
- 每次修改 `plugin` 目录内的运行逻辑或界面，都必须递增 `plugin/manifest.json` 版本号。
- 每次交付插件变更时，必须在回复中明确备注新版本号。
- 插件弹窗右上角直接读取并显示当前实际加载的 Manifest 版本，作为重新加载验收依据。

### 0.6 类型映射方案库与云端同步计划

目标：同一 Ozon `description_category_id + type_id` 首次经过规则或 AI 完成字段映射后，将可复用的字段关系、转换方法和校验策略沉淀为用户专属映射方案；后续商品优先执行该方案，仅把新增或冲突字段交给 AI。随着真实使用样本增加，稳定类型应逐步不再依赖 AI。

#### 映射方案边界

- 只保存字段关系、别名、转换规则、SKU/商品作用域、字典选择策略、证据计数和置信度；不得保存某个商品的颜色、材质、品牌等具体值。
- 方案必须按用户/租户隔离，禁止不同账号之间互相读取或污染。
- 唯一身份至少包含：数据源、Ozon `description_category_id`、`type_id`、Ozon Schema 指纹、详情采集器版本和映射引擎版本。
- Ozon Schema 指纹变化、字段类型变化或字典合同变化时，原方案自动降级为待重新验证，不能继续无条件提交。

#### 版本与同步规则

1. 每次“逻辑映射方案”发生有效变动后，先在本地事务中生成不可变 revision，并写入同步 Outbox；输入框编辑过程或重复等价结果不产生新 revision。
2. 桌面端通过用户认证后的后端接口上传 revision；云端使用幂等键避免网络重试产生重复版本。
3. 云端已有同一映射身份时，以新 revision 更新当前生效版本；同时保留历史版本和变更摘要，支持审计与一键回滚，不做不可恢复的物理覆盖。
4. 使用乐观并发版本号或 ETag 防止多台电脑互相覆盖；发现云端版本已前进时必须进入合并/冲突流程。
5. 离线时继续使用本地方案并保留 Outbox；恢复网络后按 revision 顺序补传。应用启动、登录或选择类型时拉取云端最新生效版本。
6. 云端只存映射方案及匿名聚合验证数据，不上传 Ozon Key、Chrome Cookie、Authorization、原始详情 JSON 或商品敏感数据。

#### 成熟度与 AI 退场

- `draft`：首次 AI/规则生成，只有单样本证据。
- `candidate`：已通过多个不同商品或供应商验证，但仍允许 AI 增量补全。
- `stable`：连续验证通过且无未解决必填字段，默认不调用 AI。
- `revalidation_required`：Schema、采集器或映射引擎版本变化，或运行校验失败，重新启用 AI 差异分析。
- AI 只能提交候选 revision；字典 ID、数据类型、单位、取值数量和必填覆盖通过本地确定性校验后，revision 才能提升成熟度并成为当前生效版本。

#### 已确认的 AI 参与流程

首次为某个 Ozon 类型建立映射方案时：

1. 应用仍通过 Chrome 扩展自行采集 1688 详情页 JSON，不把页面访问和原始数据采集交给 AI。
2. 应用将原始详情 JSON 规范化为带不可变 `factId` 的 `ProductFact`；每条事实必须保留原始标签、值、来源位置、商品/SKU 作用域和置信度。
3. 应用读取用户已选 `description_category_id + type_id` 对应的 Ozon 属性 Schema 与字典，先运行精确字段、别名、数据类型和既有缓存等确定性规则。
4. 只把尚未解决的目标属性、与其相关的 `ProductFact`、本地筛选后的 Ozon 字典候选和必要上下文发送给 AI；不得默认发送完整页面 JSON 或无关字段。
5. AI 返回严格结构化的候选映射决定，不直接生成或提交最终 Ozon 商品请求。每个决定必须引用已有 `factId`，字典值只能从应用提供的候选中选择，禁止编造事实或字典 ID。
6. 本地确定性编译器验证事实存在性、字典成员关系、数据类型、单位、基数、商品/SKU 作用域和必填覆盖；校验失败的 AI 结果不得激活。
7. 通过校验的决定生成 `draft` 状态的 `TypeMappingProfile` revision，并立即在当前商品上执行。首个样本不得直接将方案提升为 `stable`。
8. revision 在本地事务中保存并写入同步 Outbox，随后按既定版本规则同步云端。

后续处理同一类型的其他商品时：

1. 优先加载当前 `TypeMappingProfile` 和 `ValueMappingCache`，在本地执行映射与校验。
2. `MappingGapAnalyzer` 检查缺失必填事实、未知源字段、歧义或冲突、未知字典值、类型/单位错误及商品/SKU 作用域冲突。
3. 没有缺口时不调用 AI；存在缺口时，只把未解决差异、相关事实、候选字典和当前 `baseRevision` 发送给 AI。
4. AI 返回相对 `baseRevision` 的 `MappingPatch`，不得重写完整方案。
5. Patch 通过本地确定性校验后生成新的不可变 revision、重新执行当前商品并同步云端；未通过时保留原生效版本并输出可诊断错误。

以下情况会重新启用 AI 增量分析：必填源事实缺失、出现未知 1688 字段标签、候选事实歧义或冲突、出现无法由缓存确认的新字典值、类型或单位校验失败、商品/SKU 作用域冲突、Ozon Schema 指纹变化，或同一映射连续执行失败。

#### 双知识库边界

- `TypeMappingProfile` 保存“Ozon 属性与 1688 语义事实如何对应”：字段关系、源标签别名、作用域、转换方法、字典解析策略、验证证据和成熟度。
- `ValueMappingCache` 保存“某个规范化源值如何对应 Ozon 标准值或字典 ID”，并绑定 Ozon 属性 ID、字典合同版本和语言环境。
- 遇到新的颜色、材质等商品值时，应优先扩展 `ValueMappingCache`，不能因为值变化就改写字段关系；只有字段含义、作用域或转换规则发生变化时才创建新的 `TypeMappingProfile` revision。
- 两类知识库都不得把某个商品的值保存为后续商品的默认值。

#### 实施前必须冻结的数据合同

- `ProductFact`：至少包含 `factId`、原始标签、原始值、规范化值、来源类型、来源定位、`common/sku` 作用域、可选 `skuId` 和置信度。
- `TypeMappingProfile`：至少包含映射身份、Schema/采集器/引擎版本、不可变 revision、成熟度、目标属性映射规则、转换与字典策略、验证统计和激活状态。
- `MappingPatch`：至少包含 `baseRevision`、受影响的目标属性、受限操作集合、引用的 `factId`、理由与置信度；只允许声明式变更，不允许携带可执行代码或任意脚本。
- AI 请求与响应都必须采用版本化的严格 JSON Schema；解析失败、未知字段、越权操作或引用不存在时一律拒绝，不做宽松猜测。

#### 验收门禁

1. 同一逻辑变更在断网重试后，云端只产生一个 revision。
2. 云端当前版本可被新 revision 替换，且任意历史版本可以恢复。
3. 多设备同时修改不会静默覆盖。
4. 类型方案不得包含任何具体商品值或凭据。
5. `stable` 方案可在不调用 AI 的情况下处理同类型新商品；出现未知字段时只调用 AI 处理差异。
6. Schema 指纹改变后，旧方案不会继续自动提交 Ozon。
7. 首个商品只能生成 `draft` 方案，不能因单样本验证通过自动提升为 `stable`。
8. 同类型新商品无映射缺口时，整个流程不会发起 AI 请求。
9. 仅出现新商品值时，只更新 `ValueMappingCache`；字段关系未变化就不创建新的类型映射 revision。
10. AI 引用不存在的 `factId` 或未提供的字典 ID 时，候选方案或 Patch 必须被拒绝且不能激活。
11. 后续补缺请求只提交相对 `baseRevision` 的差异，AI 不返回或覆盖完整方案。
12. 每次有效逻辑变更只产生一个本地 revision，并最终同步为一个云端 revision。

## 历史方案附录：Python 脚本启动器（2026-08-24）

以下内容仅用于保存历史决策背景，不再作为当前产品范围或桌面端实现依据。

### 1. 已冻结的核心需求

1. Auto Magic 不再提供低代码流程设计能力。
2. 用户只需要选择一个已有的 `.py` 文件并运行。
3. Python 代码由用户在 VS Code、PyCharm 等外部工具中编写和调试。
4. 保留 Auto Magic Chrome 扩展，用于直接控制用户当前正在使用的 Chrome。
5. 自动化脚本复用当前 Chrome 的登录状态、Cookie、标签页和页面上下文。
6. Auto Magic 不内置 Python 代码编辑器。

### 2. MVP 界面

主界面只保留以下功能：

- 选择 Python 脚本；
- 显示脚本绝对路径；
- 显示 Chrome 扩展连接状态；
- 显示当前可自动化标签页标题和地址；
- “运行”按钮；
- “停止”按钮；
- 实时标准输出、错误输出、开始时间、结束时间和退出码；
- 清空当前日志。

应用启动后直接显示主界面，不再显示项目创建、流程设计或元素编辑页面。

### 3. 明确删除的旧范围

以下能力不再建设，也不保留兼容层：

- 可视化流程设计器；
- 指令库和拖拽编排；
- If、For、Try 等可视化控制节点；
- 指令配置弹窗；
- 流程变量和全局变量面板；
- 流程项目树；
- 元素库、元素分组和元素编辑器；
- 元素拾取器和相似元素分析；
- `.flow` 合同及 Astron 流程转换；
- 流程级撤销、重做、静态校验和单步调试；
- 旧项目和旧流程文件迁移。

### 4. 目标架构

```text
Auto Magic 桌面启动器
  ├─ 选择/启动/停止 Python
  ├─ 管理脚本进程树
  ├─ 转发 stdout/stderr
  └─ 托管本机安全 Bridge
             │
             ├── Python Auto Magic SDK
             │      └─ 发送浏览器操作请求
             │
             └── Chrome 扩展
                    └─ 操作当前 Chrome 标签页和 DOM
```

#### 4.1 桌面启动器

- 使用轻量 Windows 桌面界面；
- 校验所选文件必须是本地 `.py` 文件；
- 以脚本所在目录作为默认工作目录；
- 捕获并显示脚本标准输出和错误输出；
- 停止时终止脚本及其子进程树；
- 同一时刻只允许运行一个脚本；
- Chrome 扩展未连接时禁止启动依赖浏览器的脚本，并给出中文提示。

#### 4.2 本机 Bridge

- 只监听 `127.0.0.1`；
- 每次应用启动生成临时会话令牌；
- Python SDK 和 Chrome 扩展必须完成协议握手；
- 所有请求携带请求 ID，并返回成功结果或结构化错误；
- 应用关闭后扩展保持静默重试，不产生 Chrome 扩展错误页；
- Bridge 断开时，正在等待的 Python 调用必须立即失败，不能无限等待。

#### 4.3 Chrome 扩展

- 连接当前用户 Chrome，不启动新的浏览器；
- 默认操作当前活动的 HTTP/HTTPS 页面；
- 内部页、扩展页和 Chrome 设置页明确拒绝自动化；
- 扩展只承担连接、标签页选择、DOM 操作和 JavaScript 执行；
- 不再包含元素库、元素编辑器和复杂拾取状态机；
- 扩展重新加载后，已打开页面的内容脚本可以自动恢复；
- 权限保持最小化，并在安装说明中明确列出。

#### 4.4 Python SDK

Python 脚本通过稳定 SDK 调用当前 Chrome，而不是自行拼装 WebSocket 消息。

预期使用方式：

```python
from automagic import chrome

page = chrome.current_tab()
page.goto("https://example.com/orders")
page.fill('input[name="keyword"]', "测试订单")
page.click('button[type="submit"]')
page.wait_for(".result-table")

rows = page.query_all(".result-table tbody tr")
for row in rows:
    print(row.text)
```

首批 SDK 能力：

- 获取当前标签页；
- 获取标签页列表并按 ID 选择；
- 打开、跳转、刷新和关闭标签页；
- 等待页面加载；
- CSS/XPath/文本定位；
- 点击、悬停、输入、选择和滚动；
- 等待元素出现、消失或可交互；
- 读取文本、属性和值；
- 查询单个或多个元素；
- 执行 JavaScript；
- 页面和元素截图；
- 统一超时与中文错误信息。

### 5. Python 环境策略

该项尚待最终确认。当前推荐默认策略：

1. 优先检测脚本目录下的 `.venv\Scripts\python.exe`；
2. 未检测到 `.venv` 时使用 Auto Magic 内置 Python；
3. SDK 自动加入运行环境，不要求用户手工修改 `PYTHONPATH`；
4. 首版不提供图形化依赖安装器，第三方依赖由用户在外部终端或 IDE 中维护。

### 6. 错误处理

- Python 文件不存在或扩展名错误：禁止运行；
- Python 解释器不可用：禁止运行并显示实际检测路径；
- Chrome 扩展未连接：禁止浏览器调用；
- 当前标签页不可自动化：SDK 抛出明确异常；
- CSS/XPath 未匹配：返回定位器、页面地址和超时信息；
- 脚本异常退出：保留完整 stderr 和退出码；
- 用户停止：终止进程树并记录“用户停止”；
- Auto Magic 关闭时脚本仍在运行：提示用户停止后关闭。

### 7. 实施阶段

#### Phase 0：重新建立仓库骨架

- 建立最小解决方案和目录结构；
- 冻结桌面端、Bridge、扩展和 Python SDK 四个合同；
- 建立构建、测试和本地启动脚本；
- 不迁移旧流程设计器代码。

#### Phase 1：脚本启动器

- 完成脚本选择、路径记忆、运行和停止；
- 完成 stdout/stderr 实时日志；
- 完成 Python 环境检测；
- 完成单实例运行和进程树清理；
- 完成浅色、深色基础界面。

#### Phase 2：当前 Chrome Bridge

- 重建最小 Chrome 扩展；
- 完成扩展连接状态和当前标签页状态；
- 完成请求关联、超时、断线和重连；
- 完成页面注入和基础 DOM 操作；
- 确保当前 Chrome 登录状态可以直接复用。

#### Phase 3：Python SDK

- 完成标签页、页面、元素和 JavaScript API；
- 完成统一异常类型和类型提示；
- 提供可直接运行的示例脚本；
- 提供 SDK 单元测试和扩展协议集成测试。

#### Phase 4：验收和交付

- 完成真实已登录网站的人工门禁；
- 完成扩展离线、重载和断线恢复门禁；
- 完成长时间运行、脚本异常和强制停止门禁；
- 生成安装包、扩展目录和中文使用说明。

### 8. MVP 验收标准

1. 用户可以选择任意本地 `.py` 文件并启动。
2. 脚本输出可以实时显示中文和英文内容，编码无乱码。
3. 用户可以停止脚本，且不存在残留子进程。
4. Chrome 扩展可以连接用户当前 Chrome。
5. Python 脚本可以操作当前已登录页面，不要求再次登录。
6. 普通点击、输入、等待、读取和 JavaScript 执行可用。
7. Chrome 内部页不会被误判为可自动化页面。
8. 扩展未连接、页面不支持、元素未找到和脚本异常均有明确错误。
9. Auto Magic 未启动时，扩展不产生持续错误记录。
10. 关闭并重新打开 Auto Magic 后，可以重新选择并运行脚本。

### 9. 风险与边界

- 用户在脚本运行期间操作同一标签页，可能与自动化互相干扰；
- 网站 DOM 更新后，硬编码定位器需要开发者维护；
- 跨域 iframe、受限 Shadow DOM 和 Chrome 内部页需要明确边界；
- 登录过期、验证码和二次认证仍需用户处理；
- 扩展拥有网页访问权限，Bridge 必须限制为本机并使用临时令牌；
- 用户脚本属于本地可信代码，Auto Magic 不提供沙箱隔离。

### 10. 待确认事项

1. Python 环境是否采用“脚本目录 `.venv` 优先、内置 Python 兜底”；
2. 是否需要向 Python 脚本传递命令行参数；
3. 是否需要保存最近运行脚本列表；
4. 首版是否需要页面/元素截图 API；
5. 首版是否允许 Python 执行任意页面 JavaScript。

后续任何需求变化必须同步更新本文档，再开始对应代码实现。
