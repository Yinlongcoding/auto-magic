# Auto Magic 会话交接文档

> 交接日期：2026-08-24
> 工作目录：`D:\Project\auto-magic`
> 新方案主文档：`megic.md`
> 当前阶段：旧仓库已清空，新架构尚未编码

## 1. 下个会话首先执行

1. 完整阅读 `HANDOFF.md`；
2. 完整阅读 `megic.md`；
3. 不恢复旧流程设计器、元素库或旧 Chrome 拾取器代码；
4. 按“每次一个问题”的方式确认 `megic.md` 第 10 节待确认事项；
5. 需求确认完成后先同步修改 `megic.md`，再建立新代码骨架。

## 2. 产品方向已经彻底调整

Auto Magic 不再建设低代码 RPA 流程设计器。

新的产品定位：

- Auto Magic 只负责选择并运行一个已有的 Python 脚本；
- Python 代码由用户在 VS Code、PyCharm 等外部工具中硬编码；
- Auto Magic 不提供内置代码编辑器；
- 保留 Chrome 扩展；
- Python 脚本通过 Auto Magic SDK 和本机 Bridge 调用扩展；
- 扩展直接操作用户当前正在使用的 Chrome；
- 自动化可以复用用户当前 Chrome 的登录状态、Cookie 和标签页。

## 3. 已确认需求

以下结论无需再次讨论，除非用户明确提出变更：

1. 自动化逻辑全部由 Python 硬编码；
2. Auto Magic 只是脚本启动器，不是流程设计器；
3. Auto Magic 只选择和运行 `.py` 文件；
4. Python 文件在外部 IDE 中编写；
5. Chrome 扩展可以保留；
6. 必须能够操作用户当前 Chrome，而不是强制启动专用浏览器；
7. 当前 Chrome 的既有登录状态需要被自动化直接复用；
8. 旧方案和旧代码无需兼容或迁移。

## 4. 明确退出范围

下列旧功能已经取消：

- 可视化流程设计器；
- 指令搜索、拖入、排序和嵌套；
- If、For、Try 等流程控制节点；
- 指令配置弹窗；
- 元素库、元素分组和元素编辑器；
- 元素拾取和相似元素捕获；
- 全局变量、流程变量和变量映射；
- `.flow` 文件和 Astron 转换；
- 流程撤销、重做、静态校验、调试和单步运行；
- 旧项目格式迁移。

## 5. 仓库当前状态

用户已明确要求清空当前方案和代码仓库，操作已经完成。

清理结果：

- 保留：`.git`；
- 新建：`megic.md`；
- 新建：`HANDOFF.md`；
- 已删除：旧源码、扩展、运行时、测试、文档、工具、产物和依赖缓存；
- 旧文件中只有 Git 已提交内容可从历史恢复；未提交内容不能依靠当前仓库恢复。

当前 Git 工作区预期状态：

```text
 D README.md
?? megic.md
?? HANDOFF.md
```

不要执行 `git reset --hard`、`git checkout -- .` 或其他恢复旧方案的操作。

## 6. 新架构摘要

```text
Auto Magic 桌面启动器
  ├─ 选择 Python 脚本
  ├─ 启动/停止脚本进程树
  ├─ 显示 stdout/stderr 和退出码
  ├─ 显示 Chrome 扩展及当前标签页状态
  └─ 托管本机安全 Bridge
             │
             ├── Python Auto Magic SDK
             │      └─ 发起页面和 DOM 操作
             │
             └── Chrome 扩展
                    └─ 操作当前 Chrome
```

计划中的首批浏览器能力：

- 获取当前标签页和标签页列表；
- 打开、跳转、刷新和关闭标签页；
- CSS、XPath 和文本定位；
- 点击、悬停、输入、选择和滚动；
- 等待页面和元素状态；
- 读取文本、属性和值；
- 查询单个或多个元素；
- 执行 JavaScript；
- 页面或元素截图；
- 统一超时和结构化异常。

## 7. 待确认事项

下个会话按顺序、每次只询问一个问题：

1. Python 环境是否采用“脚本目录 `.venv` 优先，Auto Magic 内置 Python 兜底”；
2. 是否需要由界面向脚本传递命令行参数；
3. 是否保存最近运行过的脚本列表；
4. 首版是否需要页面和元素截图 API；
5. 首版是否允许 Python 执行任意页面 JavaScript。

当前推荐答案：

- 自动检测 `.venv`，内置 Python 兜底；
- MVP 暂不设计复杂参数表单；
- 保存少量最近脚本路径；
- 保留截图 API；
- 允许执行 JavaScript，但必须经过本机会话令牌验证。

## 8. 推荐实施顺序

### Phase 0：最小仓库骨架

- 新建最小解决方案；
- 建立桌面启动器、Bridge、Chrome 扩展、Python SDK 四个模块；
- 冻结进程协议和浏览器协议；
- 建立构建、测试和启动脚本。

### Phase 1：Python 脚本启动器

- 脚本选择；
- Python 环境解析；
- 运行、停止和进程树清理；
- stdout/stderr 实时显示；
- 退出码和运行状态。

### Phase 2：当前 Chrome Bridge

- 最小 Chrome 扩展；
- 本机回环连接和临时令牌；
- 当前标签页解析；
- 内容脚本注入；
- DOM 操作与断线恢复。

### Phase 3：Python SDK

- 稳定的 Python API；
- 类型提示和统一异常；
- 示例脚本；
- SDK、Bridge 和扩展集成测试。

### Phase 4：真实验收

- 使用当前已登录 Chrome 页面执行脚本；
- 验证扩展离线静默；
- 验证扩展重载和页面脚本恢复；
- 验证异常、超时和强制停止无残留进程。

## 9. 开发纪律

- `megic.md` 是新阶段唯一方案事实来源；
- 任意需求变更必须先更新 `megic.md`；
- 不引用旧 `RAP_PLAN_1.md`，该文件已经删除；
- 不以旧代码为兼容目标；
- Chrome 扩展只连接 `127.0.0.1`，并使用临时会话令牌；
- 扩展未连接或目标页不支持时必须快速失败，不能无限等待；
- 所有用户可见文本必须中文化；
- 每个实施阶段完成后必须执行自动化测试和真实 Chrome 门禁。

## 10. 给下个会话的一句话

从 `megic.md` 的待确认事项开始逐个提问，确认后从空仓库搭建“Python 脚本启动器 + 当前 Chrome 扩展 Bridge”，不要继续修复或恢复旧流程设计器。
