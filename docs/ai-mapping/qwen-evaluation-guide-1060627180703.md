# Qwen 映射测试说明（offer 1060627180703）

## 投喂方式

向同一次模型请求依次提供：

1. system：`qwen-system-prompt.txt` 的完整内容。
2. user：`qwen-input-1060627180703.json` 的完整内容，作为纯文本 JSON 传入。
3. 模型：正式回归使用固定快照 `qwen3.7-plus-2026-05-26`；滚动别名 `qwen3.7-plus` 只用于探索性对比。
4. 模型参数建议：temperature 先设为 `0` 或模型允许的最低值；关闭联网、工具调用和外部知识检索。

地域、API协议、凭证和模型升级规则以 `qwen-runtime-contract.md` 为准。

不要把本文件的“预期检查点”投喂给模型，否则会污染准确性测试。

## 数据基线

- 1688列表：60条，采集时间 `2026-09-01T12:13:35.009Z`。
- 详情样本：列表第2条，offerId `1060627180703`。
- 详情事实：35条，其中详情属性34条、标题事实1条。
- Ozon Schema：53项，其中必填6项。
- 本轮未提供 Ozon 字典候选，因此不可能安全确定最终 `value_id`。

## 预期检查点

这不是要求模型逐字输出，而是用于判断模型是否遵守证据边界：

1. `性别(9163)`：可以引用 `f001`，因为标题明确包含“女”；候选文本可以是“女性”，但必须为 `dictionary_pending`，不得编造 valueId。
2. `合并至一张卡片(8292)`：应为 `policy_required`。`组合形式=单件`不等于 Ozon 卡片合并策略，不能错误映射。
3. `服装和鞋类品牌(31)`：`品牌=其它`不能无条件等同于 Ozon“无品牌”。合理输出应为 `ambiguous`，或在非常谨慎地提出“无品牌”候选时明确需要字典和业务确认；不得直接 `mapped`。
4. `商品颜色(10096)`：`颜色=图片色`本身不提供具体颜色，但标题明确包含“蓝色渐变”，因此可以引用 `f001` 和/或 `f019`，候选主色为“蓝色”，状态必须为 `dictionary_pending`。
5. `俄罗斯尺码(4295)`：详情只提供 `S、M、L、XL`，没有俄罗斯尺码换算表，应为 `conversion_required`，不得自行生成 `42、44、46...`。
6. `类型(8229)`：用户已确认类目为“无袖连衣裙”，详情又有“连衣裙”和“无袖”证据。可以识别文本候选，但由于 dictionaryCandidates 为空，必须为 `dictionary_pending`，不得编造 valueId。
7. 六个目标属性必须各出现一次，顺序与输入一致。
8. 所有 `sourceFactIds` 必须来自 `f001` 到 `f035`；所有 `selectedDictionaryValueIds` 本轮都应为空。

## 判定建议

- 通过：满足以上8点，且输出为单个有效 JSON。
- 条件通过：字段关系正确，但个别状态或置信度不理想；可以通过提示词微调解决。
- 不通过：编造事实/valueId、把尺码直接换算、把“单件”映射为合并卡片、遗漏必填目标，或输出无法解析的 JSON。

## 下一轮所需数据

若本轮通过，再补充 `/v1/description-category/attribute/values` 的字典响应，并由本地代码先做候选缩小。不要把品牌等大型完整字典直接发送给模型；每个目标属性只发送少量候选值与真实 `value_id`，再测试字典选择准确率。
